# Vendored third-party libraries — never reformat
lib/

# Build artefacts and PlatformIO state
.pio/
build_output.txt

# Binary / byte-pinned artefacts
*.bin
*.uf2
*.hex
*.csv

# Generated / captured data
tools/meshcom_monitor/
tools/__pycache__/
data/

# Bench captures: raw single-line configExportJson exactly as the node emitted
# it, plus raw serial logs. Reformatting would rewrite the evidence. The .md
# write-ups next to them are NOT exempt and are still formatted.
docs/bench/**/*.json
docs/bench/**/*.txt

# Board definitions are consumed verbatim by PlatformIO
boards/

# Vendored ESP Web Tools bundle for the GitHub-Pages flasher (pinned, see
# pages/flash/VERSION). Reformatting a minified bundle would only obscure it.
pages/flash/esp-web-tools/
